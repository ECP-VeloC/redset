name: redset build and test

permissions:
  actions: write

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        target: [Debug, Release]

    name: ${{ matrix.os }}-${{ matrix.target }}

    runs-on: ${{ matrix.os }}

    steps:

      - name: checkout
        uses: actions/checkout@v2
        with:
          path: redset
          fetch-depth: 0

      - name: get deps
        uses: ecp-veloc/github-actions/get-scr-os-deps@main
        with:
          os: ${{ matrix.os }}

      - name: install kvtree
        uses: ecp-veloc/github-actions/build-ecp-veloc-component@main
        with:
          component: kvtree

      - name: install rankstr
        uses: ecp-veloc/github-actions/build-ecp-veloc-component@main
        with:
          component: rankstr

      - name: build and test
        uses: ecp-veloc/github-actions/cmake-build-test@main
        with:
          component: redset
          target: ${{ matrix.target }}
          cmake_line: "-DBUILD_SHARED_LIBS=ON -DREDSET_LINK_STATIC=ON"
